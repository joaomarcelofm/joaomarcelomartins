<div class="row">
  <% @projects.each do |project| %>
  <div class="col-md-4">
    <div class="project-card">
      <%= image_tag project.image %>
      <h3><%= project.name %></h3>
      <p>
        <%= project.description %>
      </p>
    </div>
  </div>
  <% end %>
</div>
